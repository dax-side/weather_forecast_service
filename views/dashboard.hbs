<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .weather-card { margin: 20px 0; }
        .search-history { max-height: 400px; overflow-y: auto; }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8">
                <h1 class="mb-4">🌤️ Weather Forecast Service</h1>

                {{#if error}}
                <div class="alert alert-danger">
                    <strong>Error:</strong> {{error}}
                </div>
                {{/if}}

                <div class="card weather-card">
                    <div class="card-header">
                        <h5>Search Weather</h5>
                    </div>
                    <div class="card-body">
                        <form action="/search" method="POST">
                            <div class="mb-3">
                                <label for="cityName" class="form-label">City Name</label>
                                <input type="text" class="form-control" id="cityName" name="cityName"
                                       placeholder="Enter city name (e.g., London, New York)" required>
                            </div>
                            <button type="submit" class="btn btn-primary">Get Weather</button>
                        </form>
                    </div>
                </div>

                <div class="text-muted mt-3">
                    <small>
                        💡 <strong>API Usage:</strong> You can also use our REST API endpoints:<br>
                        • <a href="/api/docs" target="_blank">API Documentation</a><br>
                        • <code>GET /api/weather/current?city=London</code><br>
                        • <code>GET /api/weather/forecast?city=London</code>
                    </small>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <h6>Recent Searches</h6>
                    </div>
                    <div class="card-body search-history">
                        {{#if history}}
                        <ul class="list-group list-group-flush">
                            {{#each history}}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                {{cityName}}
                                <small class="text-muted">{{timestamp}}</small>
                            </li>
                            {{/each}}
                        </ul>
                        {{else}}
                        <p class="text-muted">No recent searches</p>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>