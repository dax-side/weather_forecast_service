<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #f8f9fa; }
        .weather-card { margin: 20px 0; }
        .temperature { font-size: 3rem; font-weight: bold; }
        .weather-icon { font-size: 4rem; }
        .forecast-item { margin: 10px 0; padding: 15px; border-radius: 8px; }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1>{{title}}</h1>
                    <a href="/" class="btn btn-secondary">← Back to Dashboard</a>
                </div>

                {{#if success}}
                <div class="card weather-card">
                    <div class="card-body text-center">
                        <div class="weather-icon mb-3">
                            <i class="fas fa-sun" style="color: #ffc107;"></i>
                        </div>
                        <div class="temperature mb-2">{{weather.temperature}}°C</div>
                        <h4>{{weather.description}}</h4>
                        <p class="text-muted">{{weather.cityName}}, {{weather.country}}</p>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h6>Weather Details</h6>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-6">
                                        <strong>Feels like:</strong><br>
                                        {{weather.feelsLike}}°C
                                    </div>
                                    <div class="col-6">
                                        <strong>Humidity:</strong><br>
                                        {{weather.humidity}}%
                                    </div>
                                </div>
                                <div class="row mt-2">
                                    <div class="col-6">
                                        <strong>Wind Speed:</strong><br>
                                        {{weather.windSpeed}} m/s
                                    </div>
                                    <div class="col-6">
                                        <strong>Pressure:</strong><br>
                                        {{weather.pressure}} hPa
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h6>5-Day Forecast</h6>
                            </div>
                            <div class="card-body">
                                {{#if weather.forecast}}
                                {{#each weather.forecast}}
                                <div class="forecast-item bg-light">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div>
                                            <strong>{{date}}</strong><br>
                                            <small>{{description}}</small>
                                        </div>
                                        <div class="text-end">
                                            <div class="temperature">{{maxTemp}}°/{{minTemp}}°</div>
                                        </div>
                                    </div>
                                </div>
                                {{/each}}
                                {{else}}
                                <p class="text-muted">Forecast data not available</p>
                                {{/if}}
                            </div>
                        </div>
                    </div>
                </div>
                {{else}}
                <div class="alert alert-danger">
                    <strong>Search Error:</strong> {{error}}
                </div>
                {{/if}}

                <div class="text-muted mt-3">
                    <small>
                        💡 <strong>API Usage:</strong> You can also use our REST API:<br>
                        • <a href="/api/docs" target="_blank">API Documentation</a><br>
                        • <code>GET /api/weather/current?city={{#if weather.cityName}}{{weather.cityName}}{{else}}London{{/if}}</code>
                    </small>
                </div>
            </div>
        </div>

        {{#if history}}
        <div class="row mt-4">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <h6>Recent Searches</h6>
                    </div>
                    <div class="card-body search-history" style="max-height: 200px; overflow-y: auto;">
                        <ul class="list-group list-group-flush">
                            {{#each history}}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                {{cityName}}
                                <small class="text-muted">{{timestamp}}</small>
                            </li>
                            {{/each}}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        {{/if}}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>